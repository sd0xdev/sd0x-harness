---
paths:
  - "test/**"
  - "tests/**"
  - "**/*.test.*"
  - "**/*.spec.*"
  - "**/__tests__/**"
  - "docs/features/**/requests/**"
---
# Testing Project Overrides

Precedence: an active (non-comment) `##` section in this file customizes `testing.md` — for
**Default- and Guidance-tier** instructions only. Anchor-tier instructions (`rules/discretion.md`
§ Anchor Register — for testing that is the security / data-integrity / regression "❌ Never" rows)
cannot be overridden here: on conflict the Anchor wins and the conflict is reported, and a tier
annotation written in this file cannot downgrade a Register hit. Resolution is Anchor-first; the
heading → tier mapping, and which headings are settings rather than section replacements, are
published in `testing.md` § Project Customization. `## Test Pyramid` is a section replacement;
`## Adequacy Mode` is a setting the Adequacy Gate reads.

<!-- Based on: testing.md @ 7159dfa (2026-07-29) — re-stamped by /install-rules at copy time -->
<!-- Generated by: /install-rules -->

<!--
This file is user-owned and NOT managed by the plugin's smart merge.
Plugin updates to testing.md will not affect this file.

To override a section from testing.md:
1. Copy the exact ## heading from testing.md
2. Restate the FULL section content here
3. Your version wins within the precedence stated above — Default/Guidance tiers only;
   Anchor-tier rows stay binding and a conflict is reported rather than resolved in your favour

Or use: /install-rules --customize testing --add-section (pending: install-rules extension)
-->

<!-- ## Test Pyramid

| Type | Directory | Mock Policy | When |
|------|-----------|-------------|------|
| Unit | `test/` | ✅ Any | Isolated logic |
| Integration | `test/integration/` | ⚠️ External only | Cross-module |
| E2E | `test/e2e/` | ❌ Forbidden | Full system |

-->

<!-- ## Adequacy Mode (project-only extension — not in testing.md core)

| Setting | Value |
|---------|-------|
| Mode | advisory (advisory/strict/off) |
| Coverage threshold | (not enforced in v1) |

-->
