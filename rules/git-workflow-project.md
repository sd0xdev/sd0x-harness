# Git Workflow Project Overrides

Precedence: an active (non-comment) `##` section in this file customizes `git-workflow.md` — for
**Default- and Guidance-tier** instructions only. Anchor-tier instructions (`rules/discretion.md`
§ Anchor Register #4 — forbidden operations, workflow grants, attribution, default protected
branches, push safety) cannot be overridden here: on conflict the Anchor wins and the conflict is
reported, and a tier annotation
written in this file cannot downgrade a Register hit. Resolution is Anchor-first; the heading → tier
mapping is published in `git-workflow.md` § Project Customization. Every heading below is a
**setting** — no section of `git-workflow.md` is replaced from here.

<!-- Based on: git-workflow.md @ ca25087 (2026-09-24) — re-stamped by /install-rules at copy time -->
<!-- Generated by: /install-rules -->

<!-- User-owned; the plugin's smart merge never touches it. Uncomment a value under a heading to
     set it. Headings left empty keep the defaults in git-workflow.md. -->

## Branch Naming

<!-- Free text replacing git-workflow.md's `Branches:` line, e.g.
     feature/<ticket>-<slug> | hotfix/* | release/* -> main -->

## Commit Format

<!-- Free text replacing git-workflow.md's `Commit:` line, e.g.
     <type>(<scope>): <subject> [TICKET-123] -->

## Protected Branches

<!-- Additions to the default protected set (main, master, develop, release/*). One bullet per
     branch: an exact name or a `<prefix>/*` pattern. There is no removal syntax: leaving a default
     out changes nothing, and a line that tries to remove one (`- !main`) is a parse error that makes
     every branch read as protected until it is fixed. Read by scripts/protected-branches.sh.
- staging
- hotfix/*
-->

## Offer Mode

<!-- on (default) · commit-only · off — whether the model offers commit/push menus once the
     gates pass (git-autonomy R4; until it ships no offer acts on this, though /claude-health validates it). Bare value, no trailing comment. -->

<!-- on -->

## Deploy Workflow

<!-- Your release flow, one step per line, run only by /deploy-flow (git-autonomy R5; until it
     ships nothing runs these, though /claude-health validates them) and only after a per-step approval that names the step. Two forms; tokens separated by single spaces, no quoting:
       merge <source> -> <target> [--no-ff|--ff-only]
       run <repo-relative-path> [args…]
     <source>/<target> are a branch or a `<prefix>/*` pattern; every `run` token must match
     ^[A-Za-z0-9._/@:=+,-]+$. Anything else is a parse error and the whole block is ignored.

```text
merge develop -> release/*
run scripts/deploy.sh staging
```
-->

## Run Steps

<!-- print (default) · execute — whether /deploy-flow runs declared `run` steps or prints them.
     Risk of `execute`: the script runs with your credentials and can commit, push, merge, publish
     or deploy. It bypasses the harness's own checks — /smart-commit's guaranteed attribution check
     and /push-ci's approval and protected pre-approval. Git hooks still run for its ordinary
     `git commit` and `git push` where they are installed, but the script can skip hooks (e.g.
     --no-verify) or run where none is installed, and the harness cannot tell which. -->

<!-- print -->
